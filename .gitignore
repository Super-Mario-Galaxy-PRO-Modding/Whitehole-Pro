/build
/dist
/nbproject/private/
data/objectdb.json

.idea
*.iml
src/META-INF/MANIFEST.MF

# Scratch: build/debug logs and one-off helper scripts
*.log
build_*.txt
check_files.*
fix_indent.ps1
quick-outline.txt
staleness.txt
tests_exit.txt
run_exit.txt
build_exit.txt
build_state.txt
*.bak
restore_labels.diff

# Root-anchored strays: the same class of one-off diagnostics, matched
# explicitly because the unanchored patterns above do not cover these names.
/temp_*.cpp
/git_*.txt
/test.txt
/test_diff.txt
/test_log.txt
/tests_out*.txt
/cmake_log.txt
/buildlog_utf8.txt
/ninja_config.log
/build_new.bat
/run_build*.bat
/check_build.ps1

# Stale build trees; ./build is the only canonical one.
/build_ninja/
/build-msvc/

# Scratch artifacts that were committed by accident and had to be un-tracked.
# `_*` covers the bulk of them (diagnostics dumps and one-off PowerShell
# helpers); the rest are one-off copies of real sources.
_*
*.tmp

# Reference-only sibling tools (Supernova, Takochu, bmd-bdl-export, ...):
# studied for ideas, never compiled or shipped — especially Takochu (GPL-3.0).
GalaxyTools/

# Visual Studio / IDE local state
.vs/