/build
/dist
/nbproject/private/
data/objectdb.json

.idea
*.iml
src/META-INF/MANIFEST.MF

# Scratch: build/debug logs and one-off helper scripts
*.log
build_*.txt
check_files.*
fix_indent.ps1
quick-outline.txt
staleness.txt
tests_exit.txt
run_exit.txt
build_exit.txt
build_state.txt
*.bak
restore_labels.diff

# Scratch artifacts that were committed by accident and had to be un-tracked.
# `_*` covers the bulk of them (diagnostics dumps and one-off PowerShell
# helpers); the rest are one-off copies of real sources.
_*
*.tmp

# Visual Studio / IDE local state
.vs/